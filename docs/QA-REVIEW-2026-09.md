# ריוויו QA + UX/UI - ספטמבר 2026

ריוויו מקצה-לקצה של האפליקציה כמו שהיא אחרי sprint-8 (`3f85ec8`), 59 יום לפני
היציאה (יציאה 03/11/2026, חזרה 17/06/2027).

**איך זה נבדק, בפועל:**

- הרצה אמיתית של האפליקציה ב-Chromium, viewport 390×844 ו-800×1180, כל 24
  הראוטים - סטטוס HTTP, שגיאות קונסולה, חריגות לא-תפוסות, בקשות שנכשלו,
  גלישה אופקית, גדלי מגע, שמות נגישים, תוויות לשדות, סדר כותרות, צילום מסך.
- בדיקות התנהגות: תפריט תחתון קבוע מול תוכן, התנהגות דיאלוגים (מיקוד, Escape,
  Tab, נעילת גלילה), וארבעה מסכי אופליין עם הרשת מנותקת באמת.
- מול פרויקט Supabase החי (`xeqfcrxrpfjlqhkijrwd`, קריאה בלבד): כל מדיניות
  RLS, כל הפונקציות והטריגרים, `get_advisors(security)`, ומצב הדאטה האמיתי.
- `npm run lint`, `npx tsc --noEmit`, `npx vitest run`, `npm run build`.

**מה לא נבדק, וצריך להיאמר:** לא הייתה גישה לסשן מחובר בדפדפן (אין סיסמה
לחשבון owner), ולכן כל מה שנראה בדפדפן הוא המצב ללא דאטה. הרשת בסביבת הבדיקה
חוסמת `*.supabase.co`, ולכן לא ניתן היה לתקוף את ה-REST API ישירות כ-anon -
אבל **כן** הורצה בדיקת הרשאות חיה בתוך בסיס הנתונים, בטרנזקציה שמתגלגלת
אחורה, שמדמה ילד, אורח ואנונימי. הפירוט המלא ב-`docs/SECURITY-CHECKS.md`.

---

## 1. מה תוקן בריוויו הזה

### 1.1 ה-Sheet הכריז `aria-modal` אבל לא התנהג כמודאל

`components/Sheet.tsx` הוא הבסיס של 29 טפסים ובוררים באפליקציה. נמדד על
האפליקציה הרצה:

| התנהגות | לפני | אחרי |
|---|---|---|
| Escape סוגר | לא | כן |
| Tab נשאר בתוך הדיאלוג | לא - יוצא אחרי 3 עצירות ב-`/checklists`, 20 ב-`/budget` | כן - 40 Tab ו-40 Shift+Tab נשארים בפנים |
| הדף מאחור נעול לגלילה | לא (`body` overflow: visible) | כן |
| `aria-labelledby` | חסר (רק `aria-label`) | מקושר לכותרת |
| מיקוד חוזר לכפתור שפתח | לא | כן |

נעילת הגלילה היא זו שמרגישים בטלפון: החלקה שנועדה לגלול טופס ארוך גררה את
המסך שמאחוריו. יש גם מחסנית מודאלים, כי `ItineraryScreen` מעביר הזמנה שנשמרה
ישר ל-sheet של ההוצאה - Escape אחד סוגר sheet אחד, והנעילה משתחררת רק
כשהאחרון נסגר.

*רקע:* ברירת המחדל של הדפדפן היא ש-`aria-modal` לא עושה כלום בפועל - הוא רק
אומר לקורא מסך להתעלם מהרקע. כל שאר ההתנהגות היא באחריות הקומפוננטה.

### 1.2 "יכול להיות שההתחברות פגה" נאמר גם כשלא הייתה בעיית התחברות

`getCurrentMember()` ב-`lib/data/trip.ts` התעלם מה-`error` של השאילתה והחזיר
`null`. כל מסך שנשען על member - `/pocket`, `/messages` - הסיק מזה שהמשתמש לא
מזוהה, הציג "לא הצלחנו לזהות אתכם - יכול להיות שההתחברות פגה" והציע קישור
התחברות מחדש.

בטיול מסביב לעולם הסיבה הרגילה לכישלון השאילתה היא רשת גרועה, לא סשן שפג.
וההצעה הזאת היא בדיוק הפעולה שמוחקת את הסשן ואת מה שנשמר לאופליין.

עכשיו: כישלון שאילתה זורק, `useMember` מפריד `memberFailed` מ-`member === null`,
ושני המסכים מציגים "לא הצלחנו לבדוק מי מחובר - כנראה אין חיבור כרגע" עם
ניסיון נוסף במקום התנתקות.

### 1.3 שאילתה עם 227 מזהים בשורת ה-URL

`in("day_id", dayIds)` נשלח כ-GET, כלומר כל uuid הוא ~36 תווים במחרוזת
השאילתה. המסלול האמיתי הוא **227 ימים** - בערך 8.5KB של URL, מעל שורת הבקשה
של 8KB שרוב השערים מקבלים. אופן הכישלון שקט: השאילתה חוזרת ריקה, והמסך מצייר
טיול שנראה לגמרי לא מתוכנן.

ארבעה מקומות שלחו את כל ימי הטיול בבת אחת:

| קובץ | מסך |
|---|---|
| `lib/data/itinerary.ts` `listItems` | מסלול, מפה (שניהם מעבירים את כל הימים) |
| `lib/data/homeDashboard.ts` | מסך הבית |
| `lib/data/readiness.ts` | מוכנות ליציאה |

עכשיו יש `chunkDayIds` ו-`listDayIdsWithItems` ב-`lib/data/itinerary.ts`, 100
מזהים לבקשה. `listItems` ממזג את המנות וממיין מחדש באותו סדר שהשרת החזיר
(`sort_order`, ואז `start_time` עם null בסוף), כי שרשור של כמה מנות ממוינות
אינו ממוין.

**סייג X:** לא ניתן היה לשחזר את ה-414 מסביבת הבדיקה (הרשת חוסמת את
Supabase). התיקון נכון בכל מקרה - הוא מסיר URL לא חסום - אבל אני לא יכול
להצהיר שזה כשל בפועל היום ולא רק סיכון.

---

## 2. אבטחה ותפקידים

**המצב טוב.** כל 30 הטבלאות עם RLS פעיל, ואין טבלה בלי מדיניות.

מה שאומת מול הפרויקט החי - לא בקריאת מדיניות אלא **בבדיקה בפועל**, בטרנזקציה
שמדמה ילד, אורח ואנונימי ומתגלגלת אחורה (טבלת התוצאות המלאה נוספה ל-
`docs/SECURITY-CHECKS.md`, ואומת אחריה שלא נשאר כלום):

- **אישור תמונות ילדים (חוק קשיח #3)** מוגן בבסיס הנתונים, לא רק ב-UI.
  שימו לב שהמדיניות `photos_kid_update_own` לבדה **לא** הייתה מספיקה - ה-WITH
  CHECK שלה בודק רק `uploaded_by = current_member_id()`, ולכן ילד היה יכול
  לעדכן את השורה שלו ולהדליק `status='approved'` ו-`shared_with_guests=true`.
  מה שעוצר את זה הוא הטריגרים `photos_enforce_kid_rules` (INSERT) ו-
  `photos_guard_update` (UPDATE), ושניהם פעילים. אותו דפוס ביומן
  (`journal_guard_kid`). **בבדיקה:** העלאה של ילד נכפתה ל-`pending`/לא-משותף,
  וניסיון שלו לעדכן ל-`approved` ולשיתוף עם אורחים השאיר את השורה בדיוק כפי
  שהייתה. זה עובד - אבל זה אומר שהגנת החוק הזה תלויה בטריגר ולא במדיניות, וזה
  עכשיו כתוב במפורש ב-`docs/SECURITY-CHECKS.md` כדי ששינוי עתידי בטריגרים לא
  ייראה כמו ניקיון.
- **אורח לא רואה מסמכים, תקציב או הוצאות** - אין להם מדיניות SELECT בכלל על
  `documents`, `expenses`, `budget_categories`, `bookings`, `place_options`.
- **ילד רואה מסמך רק אם `shared_with_kids = true`**, ואת הכספת רק דרך
  `document_shared_with_current_kid`.
- **ילד לא יכול לפתוח סשן בלי מכשיר פעיל** - `current_member_id()` מסרב
  לפתור member בתפקיד kid בלי שורה ב-`kid_devices` עם `revoked_at is null`.

### ממצאים פתוחים

| # | חומרה | מה | מצב |
|---|---|---|---|
| S1 | ~~בינונית~~ | ~~`recommend-diag` פרוס בפרודקשן ולא קיים ברפו.~~ **פחות חמור ממה שכתבתי.** קראתי את הקוד שרץ בפועל: הוא כבר מושבת בכוונה - מחזיר 410, בלי קריאות AI ובלי גישה לנתונים, והכותרת שלו אומרת שהוא נשאר רק כי אין API למחוק פונקציה. | ✅ **נסגר** - הקובץ נכנס ל-`supabase/functions/recommend-diag/`, כך שמה שרץ נראה גם בקוד. |
| S2 | נמוכה | **`photos_kid_update_own` WITH CHECK לא חוזר על `is_kid_of(trip_id)`.** ילד יכול היה תיאורטית להעביר תמונה שלו ל-`trip_id` אחר. | ✅ **תוקן** - מיגרציה `00033`, הוחלה ואומתה על הפרויקט. |
| S3 | ~~נמוכה~~ | ~~`message_reads_owner_all` WITH CHECK הוא `member_id = current_member_id()`.~~ **בבדיקה חוזרת זה לא באג.** ה-USING מאפשר ל-owner לקרוא ולמחוק סימוני קריאה של אחרים, וה-WITH CHECK מונע ממנו *לסמן* הודעות כנקראות בשם מישהו אחר - וזה נכון. | ⏸️ נשאר כמו שהוא, בכוונה. |
| S4 | נמוכה | **הגנת סיסמאות דלופות כבויה** ב-Supabase Auth. | ⏸️ **דורש אתכם** - מתג בקונסולת Supabase, לא בקוד. |
| S5 | מידע | `pg_net` מותקן ב-schema `public`. | ⏸️ לא נגעתי. הפונקציות והטריגרים קוראים ל-`net.http_post`; העברת ה-extension תשבור אותם עד שכל הקוראים יעודכנו, וזה לא שינוי לדחוף יחד עם תיקוני UX. |
| S6 | מידע | פונקציות ה-SECURITY DEFINER ניתנות לקריאה כ-anon דרך `/rest/v1/rpc/`. | ⏸️ **לא לתקן.** ביטול `EXECUTE` ל-anon לא מוסיף אבטחה - הפונקציות מחזירות רק מידע על הקורא עצמו - אבל כן משנה התנהגות: מדיניות RLS שקוראת להן תיכשל ב-`42501` במקום להחזיר אפס שורות. הבדיקה החיה מראה שאנונימי כבר רואה 0 שורות בכל טבלה. |

---

## 3. מצב הדאטה - מה שהריוויו גילה על הטיול עצמו

זה לא באג בקוד, אבל זו התמונה האמיתית 59 יום לפני היציאה:

| טבלה | שורות | מה זה אומר |
|---|---|---|
| `itinerary_days` | 227 | כל הטיול מחולק לימים |
| `itinerary_items` | **0** | אף יום לא מכיל פעילות אחת |
| `place_options` | 553 | בנק האפשרויות מלא ולא מומש לימים |
| `documents` | **0** | הכספת ריקה - אין דרכונים, אין ביטוח |
| `bookings` | **0** | אין הזמנות רשומות |
| `destination_facts` | 0 | `/facts` יציג "בקרוב!" לילדים |
| `kid_devices` | **0** | ראו K1 למטה |
| `guests_allowlist` | 0 | מסלול האורחים לא הופעל מעולם |
| `expenses` | 2 | |
| `checklist_items` | 24 (רשימה אחת) | |
| `phrasebook_entries` | 179 | |
| `emergency_info` | 7 | |

### K1 - כניסת ילדים מעולם לא הושלמה (חומרה גבוהה, תפקודית)

שישה קודי רישום נוצרו ב-04/09/2026, בין 11:48 ל-17:06. **כולם פגו,
`used_at` של כולם `null`.** `kid_devices` ריקה, ולשני הילדים (אלה, עומר)
`auth_user_id` הוא `null` - כלומר `register` ב-`kid-auth` לא הצליח אף פעם
ליצור להם משתמש auth.

המשמעות: לפי `current_member_id()` (מיגרציה 00024), ילד בלי מכשיר פעיל לא
נפתר כ-member בכלל. **אף ילד לא יכול להשתמש באפליקציה כרגע**, ולא רק שלא
יכול להיכנס - כל שאילתה שלו תחזור ריקה.

שימו לב שזה כשל בשלב `register`, לא בשלב `unlock`. הודעת
`email_provider_disabled` שנוספה ב-sprint-8 מטפלת בשלב מאוחר יותר, ולכן
כנראה לא תסביר את מה שקרה כאן.

**ומצאתי למה אי אפשר היה לדעת.** `registerDevice` ב-`lib/data/kids.ts` היה:

```ts
if (status !== 200 || !data.ok) throw new Error("invalid_code");
```

כלומר *כל* כישלון - 500 מ-`createUser`, כתיבה שנכשלה, ניתוק רשת - הוצג
בטאבלט כ"הקוד לא נכון או שפג תוקפו - מבקשים קוד חדש". זה מסביר בדיוק את מה
שרואים בדאטה: שישה קודים ביום אחד, כל אחד "לא עבד", אז ביקשו עוד אחד.

✅ **תוקן.** `registerDevice` מחזיר עכשיו תוצאה מובחנת - `invalid` /
`network` / `server` עם הודעת השרת עצמה - והמסך אומר מה קרה. שגיאת שרת אומרת
"הקוד בסדר, אבל החיבור נכשל בצד השרת" ומציגה את ההודעה, במקום לשלוח לבקש קוד
חדש שלא יעזור.

⏸️ **הכשל עצמו עדיין פתוח ודורש אתכם.** צריך להנפיק קוד אחד ולנסות, ועכשיו
המסך יגיד את הסיבה. החשד הראשון הוא שספק ה-Email ב-Supabase Auth כבוי - זה
מפיל גם את `admin.createUser` וגם את ה-`signInWithPassword` שבסוף ה-unlock.

---

## 4. UX / UI

### 4.1 מה עובד

24 מתוך 24 המסכים נטענים ב-200 ללא שגיאת קונסולה או חריגה. **אפס גלישה
אופקית** בשני ה-viewports. אפס כפתורים בלי שם נגיש, אפס שדות בלי תווית, אפס
תמונות בלי `alt`. `dir="rtl"` ו-`lang="he"` בשורש. סריקה של כל
`components/` ו-`app/` מצאה **אפס מחרוזות עברית מקודדות בקומפוננטות** - חוק
קשיח #4 נשמר במלואו. השפה עצמה עקבית, חמה ולא מתנשאת.

### 4.2 ממצאים

| # | חומרה | מה | מצב |
|---|---|---|---|
| U1 | בינונית | **אזורי מגע מתחת ל-44px ב-10 מתוך 24 מסכים.** הגרועים: כפתורי אימוג'י מצב-רוח ביומן 28-33px (וילדים לוחצים עליהם), כפתורי אייקון 32×32 במסלול (חיפוש, הוספת יום) ובתקציב (המרת מטבע), צ'יפים 24-29px במסמכים ובתקציב, וקישורים בגובה 20-24px. | ✅ **תוקן.** מדידה חוזרת: 0 אזורי מגע מתחת ל-40px בכל 24 המסכים. |
| U2 | בינונית | **`confirm()` מקורי ב-20 פעולות מחיקה.** בתוך PWA עצמאי זה דיאלוג מערכת שמציג את הדומיין, לא מכבד RTL, לא נראה כמו האפליקציה, וחוסם את ה-main thread. | ✅ **תוקן** - `components/ConfirmSheet.tsx`. |
| U3 | נמוכה | **לכל 24 הראוטים אותו `<title>`: "OurTrip".** | ✅ **תוקן** - `strings.pageTitles` + תבנית כותרת בשורש. |
| U4 | נמוכה | **`/ready`, `/pocket`, `/messages` נטענים בלי `<h1>`.** | ✅ **תוקן.** לכל 24 המסכים יש `h1`. |
| U5 | ~~נמוכה~~ | ~~כרטיס המרת המטבע קורא הפוך ב-RTL.~~ **הממצא הזה שגוי - אני מושך אותו חזרה.** בקריאה בקוד, לשורת ההמרה יש `dir="ltr"` מכוון (`ConverterCard.tsx:46`), וזו הצורה הנכונה למספרים וקודי מטבע. שני ה-"USD" הם דבר והיפוכו: אחד תווית חיה של מטבע המקור והשני הבורר. הסקתי את זה מצילום מסך של מצב ריק ולא מקריאת הקוד. | ❌ בוטל. הוגדל רק הבורר עצמו (37px → 40px). |
| U6 | ~~נמוכה~~ | ~~ב-800px הכרטיסים נמתחים לרוחב מלא בלי מיכל מקסימלי.~~ **גם זה שגוי.** מדדתי בדפדפן: ב-800px רוחב התוכן הוא 640px וממורכז, ב-1024px הוא 776px עם ה-side rail. כל המסכים כבר עוטפים ב-`max-w`. טעיתי בקריאת קנה המידה של צילום fullPage. | ❌ בוטל. שנשאר: מ-800px הטאבלט מקבל את הניווט התחתון ולא את ה-rail, וזו בחירת breakpoint, לא תקלה. |
| U7 | מידע | הרקע מאחורי sheet פתוח אינו `inert` / `aria-hidden`. | ⏸️ נשאר במכוון. ה-Sheet מרונדר **בתוך** `<main>`, אז `inert` על `main` היה משתק גם את ה-sheet עצמו. עם מלכודת המיקוד ו-`aria-modal` שנוספו, מקלדת וקורא מסך כבר לא מגיעים לרקע. |
| U8 | נמוכה | **תו NUL תקוע בתוך מחרוזת:** `setOpenArea(open ? "\0" : group.area)` ב-`OptionsPickerSheet.tsx:191`. `openArea` מוגדר `string \| null`, וברור שהכוונה הייתה `null`. זה "עבד" רק כי `"\0"` אקראית לא שווה לאף שם אזור, וזה גם מה שהפך את הקובץ ללא-טקסטואלי מבחינת git ו-grep. נמצא במקרה, בזמן ספירת ה-`confirm()`. | ✅ **תוקן.** |

---

## 5. אופליין ו-PWA

נבדק עם `setOffline(true)` ורענון אמיתי. כל ארבעת המסכים הקריטיים לאופליין
(חוק קשיח #6) נטענים ומציגים תוכן, עם באנר "אין חיבור לאינטרנט - חלק מהמסכים
לא זמינים":

| מסך | תוצאה |
|---|---|
| `/` היום | נטען, באנר מוצג |
| `/documents` | נטען, באנר מוצג |
| `/emergency` | נטען, באנר מוצג, ומודיע "מוצג מהעותק השמור במכשיר" |
| `/phrasebook` | נטען, באנר מוצג |
| `/budget` (לא קריטי) | נטען ומתדרדר יפה |

אין ממצאים פתוחים בקטגוריה הזאת.

---

## 6. בריאות הקוד

`npm run lint` נקי. `npx tsc --noEmit` נקי. `npx vitest run` - 210 בדיקות ב-16
קבצים, כולן עוברות. `npm run build` נקי, 26 ראוטים סטטיים. אין TODO/FIXME
פתוחים בקוד.

`npm audit --omit=dev` דיווח על 5 חבילות עם התרעות high:

| # | חומרה | מה | מצב |
|---|---|---|---|
| D1 | **גבוהה** | **`next@16.2.10` - תשע התרעות high**, ביניהן עקיפת middleware ב-App Router, SSRF ב-Server Actions ו-rewrites, בלבול cache בתשובות עם body, וחשיפה לא-מאומתת של נקודות קצה פנימיות. | ✅ **תוקן** - שודרג ל-`16.3.4` (נעוץ במדויק, כמו קודם). |
| D2 | בינונית | `postcss` ו-`sharp` הפגיעים נגררו דרך `next`; `nanoid` בנפרד. | ✅ **תוקן** - נסגרו עם השדרוג ו-`npm audit fix`. |
| D3 | נמוכה | `xlsx@0.18.5` - Prototype Pollution ו-ReDoS, **אין תיקון זמין ב-npm**. | ⏸️ **נשאר.** הגרסה המתוחזקת של SheetJS כבר לא מתפרסמת ל-npm, אז אין לאן לשדרג בלי להחליף ספרייה. החשיפה מוגבלת: הוא מפרסר רק קובץ שאתם בוחרים בעצמכם, בדפדפן שלכם, אחרי התחברות. |

אחרי הכול: `npm audit --omit=dev` נקי חוץ מ-`xlsx`.

**תופעת לוואי של השדרוג ששווה לדעת:** Next 16.3 מוסיף בלוק משלו ל-`CLAUDE.md`
בכל `next dev`, כלומר משנה קובץ שאתם כתבתם ומלכלך את עץ העבודה בכל הרצה.
כובה עם `agentRules: false` ב-`next.config.ts`.

אחרי השדרוג הורצו מחדש typecheck, lint, 210 בדיקות היחידה, build, 34 בדיקות
ה-e2e וסריקת הדפדפן המלאה על 24 המסכים - הכול עובר.

---

## 7. מה נשאר לכם

כל מה שאפשר היה לתקן בקוד תוקן. שלושת אלה דורשים גישה שאין לי:

1. **להנפיק קוד רישום אחד לילד ולנסות.** המסך יגיד עכשיו מה נכשל בדיוק. אם
   ההודעה מזכירה email או provider - זה המתג בקונסולת Supabase.
2. **להדליק Leaked Password Protection** ב-Supabase Auth (S4).
3. **למלא את הכספת.** `documents` ריקה, ואת המסך הזה צריך בדיוק כשאין רשת.

ולידיעה, כי זו התמונה 59 יום לפני היציאה ולא באג: 227 ימי מסלול עם 0 פעילויות,
553 אפשרויות בבנק שאף אחת לא הוצבה על יום, ו-0 הזמנות רשומות.

---

## 8. מדידה לפני ואחרי

| מדד | לפני | אחרי |
|---|---|---|
| אזורי מגע מתחת ל-40px | 10 מסכים | **0** |
| מסכים בלי `<h1>` | 3 | **0** |
| כותרות `<title>` ייחודיות | 1 ל-24 מסכים | **24** |
| Escape סוגר דיאלוג | לא | כן |
| מיקוד לכוד בדיאלוג | לא | כן (40 Tab לכל כיוון) |
| נעילת גלילה מאחורי דיאלוג | לא | כן |
| `confirm()` מערכת | 20 מקומות | **0** |
| התרעות npm high (prod) | 5 חבילות | 1 (`xlsx`, אין תיקון) |
| שאילתות עם URL לא חסום | 4 | **0** |
| גלישה אופקית | 0 | 0 |
| כפתורים בלי שם נגיש / שדות בלי תווית | 0 | 0 |
| lint / typecheck / 210 יחידה / build / 34 e2e | עובר | עובר |

**ביטחון בממצאים: 0.9** (עלה מ-0.85 בגרסה הראשונה, אחרי שבדיקת ההרשאות רצה
בפועל מול בסיס הנתונים במקום להיקרא מהמדיניות).

**סייגים והטיות:** עדיין לא הייתה גישה לסשן מחובר בדפדפן, ולכן לא נראה מסך אחד
עם דאטה אמיתית - כל השיפוט הוויזואלי הוא על מצבים ריקים, וזו גם הסיבה ששניים
מהממצאים שלי (U5, U6) לא שרדו בדיקה חוזרת ומשוכים בגוף הדוח. הסיכון של
ה-URL הארוך (סעיף 1.3) מחושב ולא נצפה, ומסומן X. יש לי גם הטיה מובנית לכיוון
מה שכלי מודד על פני שיפוט עיצובי - את הראשון יכולתי לספור, את השני רק להעריך.
